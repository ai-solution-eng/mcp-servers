apiVersion: v1
kind: Service
metadata:
  name: {{ include "text2image-mcp.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "text2image-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http-ui
      protocol: TCP
      name: http
  selector:
    {{- include "text2image-mcp.selectorLabels" . | nindent 4 }}