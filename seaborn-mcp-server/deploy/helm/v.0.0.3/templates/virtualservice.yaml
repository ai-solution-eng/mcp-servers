{{- if .Values.ezua.enabled }}
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: {{ include "mcp-seaborn-server.fullname" . }}-vs
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "mcp-seaborn-server.labels" . | nindent 4 }}
spec:
  gateways:
  - {{ .Values.ezua.virtualService.istioGateway }}
  hosts:
  - {{ .Values.ezua.virtualService.endpoint }}
  http:
  - match:
    - uri:
        prefix: /
    rewrite:
      uri: /
    route:
    - destination:
        host: {{ include "mcp-seaborn-server.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local
        port:
          number: {{ .Values.service.port }}
{{- end }}
