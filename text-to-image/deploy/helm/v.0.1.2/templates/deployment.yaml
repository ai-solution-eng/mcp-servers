apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "text2image-mcp.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "text2image-mcp.labels" . | nindent 4 }}
    {{- include "hpe-ezua.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "text2image-mcp.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "text2image-mcp.selectorLabels" . | nindent 8 }}
        {{- include "hpe-ezua.labels" . | nindent 8 }}
      annotations:
        sidecar.istio.io/inject: "false"
    spec:
      {{- if and .Values.certificatesPolicy .Values.certificatesPolicy.enabled }}
      initContainers:
        - name: build-ca-bundle
          image: {{ .Values.certificatesPolicy.initContainer.image | quote }}
          command:
{{ toYaml .Values.certificatesPolicy.initContainer.command | indent 12 }}
          volumeMounts:
{{- range .Values.certificatesPolicy.initVolumeMounts }}
            - name: {{ .name }}
              mountPath: {{ .mountPath }}
              {{- if .subPath }}
              subPath: {{ .subPath }}
              {{- end }}
              {{- if hasKey . "readOnly" }}
              readOnly: {{ .readOnly }}
              {{- end }}
{{- end }}
      {{- end }}

      containers:
        - name: text2image-mcp
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http-ui
              containerPort: {{ .Values.service.port }}
              protocol: TCP
          env:
{{- range $key, $val := .Values.env }}
            - name: {{ $key }}
              value: {{ $val | quote }}
{{- end }}
{{- if and .Values.certificatesPolicy .Values.certificatesPolicy.enabled }}
{{- range .Values.certificatesPolicy.env }}
            - name: {{ .name }}
              value: {{ .value | quote }}
{{- end }}
{{- end }}
          volumeMounts:
{{- if and .Values.certificatesPolicy .Values.certificatesPolicy.enabled }}
{{- range .Values.certificatesPolicy.volumeMounts }}
            - name: {{ .name }}
              mountPath: {{ .mountPath }}
              {{- if .subPath }}
              subPath: {{ .subPath }}
              {{- end }}
              {{- if hasKey . "readOnly" }}
              readOnly: {{ .readOnly }}
              {{- end }}
{{- end }}
{{- end }}
          resources:
{{ toYaml .Values.service.resources | indent 12 }}

      volumes:
{{- if and .Values.certificatesPolicy .Values.certificatesPolicy.enabled }}
{{- range .Values.certificatesPolicy.volumes }}
        - name: {{ .name }}
{{- if hasKey . "emptyDir" }}
          emptyDir: {}
{{- else if .configMap }}
          configMap:
            name: {{ .configMap.name }}
            items:
{{- range .configMap.items }}
              - key: {{ .key }}
                path: {{ .path }}
{{- end }}
{{- end }}
{{- end }}
{{- end }}